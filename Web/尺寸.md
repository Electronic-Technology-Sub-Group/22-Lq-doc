在 HTML 或 CSS 中，长度通常可以有以下几种形式：
- 固定尺寸，通常由数字+单位构成，单位通常为 `px`，`cm` 等，如 `18px`
- 百分比，表示包含元素本身大小的百分比，如 `50%`
- 纯数字：相当于像素 `18` 相当于 `18px`

一般来说，推荐在 CSS 标签中使用尺寸调整元素大小，而不是在 HTML 中使用诸如 `width`，`height`，`border` 等属性调整。
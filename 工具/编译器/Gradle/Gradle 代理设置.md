---
icon: SiGradle
---
```properties title:gradle.properties
systemProp.http.proxyHost=代理主机
systemProp.http.proxyPort=代理端口
systemProp.http.nonProxyHosts=例外, 支持通配符 * | 等

systemProp.https.proxyHost=代理主机
systemProp.https.proxyPort=代理端口
systemProp.https.nonProxyHosts=例外, 支持通配符 * | 等

systemProp.socks.proxyHost=代理主机
systemProp.socks.proxyPort=代理端口
systemProp.socks.nonProxyHosts=例外, 支持通配符 * | 等
```
